package express.test;

import static org.junit.Assert.*;

import org.junit.After;
import org.junit.Before;
import org.junit.Test;

public class CheckOrderTester {

	@Test
	public void testcheckorder(){
		/*MockOrder order= new MockOrder();
		MockCheckOrder checker=new MockCheckOrder();
		String id1="0000000001";
		String id2="das";
		assertEquals(true,checker.isOrderIDAvailable(id1));
		order.getOrder(id1);
		
		
		assertEquals(false,checker.isOrderIDAvailable(id2));
		order.getOrder(id2);*/

		
	}

}
