package express.test;

import static org.junit.Assert.*;

import org.junit.Test;

public class InitAccountTester {

	@Test
	public void test() {
		/*MockInitAccount initAccount=new MockInitAccount();
		MockBankAccount bankAccount=new MockBankAccount();
		MockOrgForManager org=new MockOrgForManager();
		MockStaffForManager user=new MockStaffForManager();
		MockInDoc inDoc=new MockInDoc();
		MockVehicle vehicle=new MockVehicle();
		assertEquals(true,initAccount.addInnerAccount(null));
		assertEquals(true,bankAccount.addBankAccount(null));
		assertEquals(true,org.setOrganizationInfo(null));
		assertEquals(true,user.addUserFromManager(null));
		assertEquals(true,inDoc.addIndoc(null));
		assertEquals(false,vehicle.addVehicleInfo(null));*/
	}

}
